#include <stdio.h>
#include <unistd.h>
#include "libft.h"
#include <string.h>
#include <stdlib.h>
int main ()
{
	// t_list  *head = malloc(sizeof(t_list));
	// head->content = "idriss";
	// head->next = NULL;

	// t_list  *str1 = malloc(sizeof(t_list));
	// head->content = "askour";
	// head->next = NULL;

	// t_list  *str2 = malloc(sizeof(t_list));
	// head->content = "yassine";
	// head->next = NULL;

	// t_list  *str3 = malloc(sizeof(t_list));
	// head->content = "saadia";
	// head->next = NULL;

	// ft_lstadd_front(NULL , str1);

	// printf("%p\n", head->next);
	// printf("%p\n", str1->next);
	// printf("%p\n", head);

	// char *src = "askour";
	// char *dst = malloc(10);

	// printf("%zu\n", ft_strlcpy(dst, src, 100));

    // ft_calloc(0, 0);

	printf("%s\n", ft_substr(NULL, 9, 0));

}